## API Resources

This directory contains the API resources for the frontend. The API resources are used to interact with the backend API. The API resources are organized by the type of resource they interact with. e.g `../api/video/index.server.ts` interacts with the video resource.
